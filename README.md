# Install
## Prerequisites
1. A working Rust installation with cargo
2. Git CLI
3. Curl CLI
4. SH shell
## Execute
To install fsysutils run `curl -fsSL https://api.falcon1k.dev/fsysutils | sh` in any terminal
## After Install
To update the program run `fysutils update`
